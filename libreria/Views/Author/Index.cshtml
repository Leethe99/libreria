@model IEnumerable<libreria.Models.dbModels.Author>

@{
    ViewData["Title"] = "Index";
}

<h1>Autores</h1>




<table class="table">
    <thead>
        <tr>
            <th>
<<<<<<< Updated upstream
                Nombre
=======
               Nombre
>>>>>>> Stashed changes
            </th>
            <th>
                Apellido
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LastName)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-secondary">Editar</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-secondary">Borrar</a>
                </td>
            </tr>
        }
	</tbody>
	
</table>
<a asp-action="Create" class="btn btn-secondary">Crear Autor</a>
